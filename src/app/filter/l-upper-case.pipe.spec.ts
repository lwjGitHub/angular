import { LUpperCasePipe } from './l-upper-case.pipe';

describe('LUpperCasePipe', () => {
  it('create an instance', () => {
    const pipe = new LUpperCasePipe();
    expect(pipe).toBeTruthy();
  });
});
